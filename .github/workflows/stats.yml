# GitHub 统计卡片 - 定时拉取并存入仓库，README 引用本地图片避免公共 API 503
# 参考: https://github.com/anuraghazra/github-readme-stats

name: 生成 GitHub 统计卡片

on:
  schedule:
    - cron: "0 */6 * * *"   # 每 6 小时
  workflow_dispatch:
  push:
    branches: [main]
    paths: [".github/workflows/stats.yml"]

jobs:
  stats:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: 检出仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 拉取 Stats 卡片
        run: |
          mkdir -p assets
          curl -fsSL -o assets/github-stats.svg \
            "https://github-readme-stats.vercel.app/api?username=AkkunYo&show_icons=true&theme=radical&hide_border=true&bg_color=0D1117&title_color=2C9CDF&icon_color=2C9CDF"

      - name: 拉取 Top Langs 卡片
        run: |
          curl -fsSL -o assets/github-top-langs.svg \
            "https://github-readme-stats.vercel.app/api/top-langs/?username=AkkunYo&layout=compact&theme=radical&hide_border=true&bg_color=0D1117&title_color=2C9CDF"

      - name: 提交并推送
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/
          git diff --staged --quiet || (git commit -m "🔄 更新 GitHub 统计卡片" && git push origin main)
